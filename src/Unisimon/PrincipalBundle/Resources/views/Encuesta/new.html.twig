{% extends "UnisimonPrincipalBundle::layout.html.twig" %}


{% block body -%}
    <h1>Creación Encuesta</h1>

    <form action="{{ path('encuesta_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Crear</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('encuesta') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
