{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pregunta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Texto_Ayuda</th>
                <th>Tipo</th>
                <th>Accioness</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pregunta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.textoAyuda }}</td>
                <td>{{ entity.tipo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pregunta_show', { 'id': entity.id }) }}">ver</a>
                    </li>
                    <li>
                        <a href="{{ path('pregunta_edit', { 'id': entity.id }) }}">editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pregunta_new') }}">
                Crear una nueva pregunta
            </a>
        </li>
    </ul>
    {% endblock %}
