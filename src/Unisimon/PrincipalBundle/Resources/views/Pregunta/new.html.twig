{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pregunta creation</h1>

    <form action="{{ path('pregunta_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Crear</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pregunta') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
